NUMBER y NUMBER(n) : Para guardar números, si tiene la (n) no entran decimales
NUMBER(m,d): Lo mismo que el superior. Ejemplo: NUMBER(4,2), guarda cuatro números, dos de los cuales son decimales

SELECT * FROM TEMA5.PERSONAS;
GRANT DBA TO TEMA4

NOT NULL: no se puede quedar como un valor vacío
CONSTRAINT__X: Sirve para darle un nombre
PRIMARY KEY
FOREIGN KEY
REFERENCE__X: Sirve para referenciar un clave externa

CHECK (SITUACIÓN A VALIDAR): Para validar algo en específico
